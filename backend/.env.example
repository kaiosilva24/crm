# Configuração do PostgreSQL (copie este arquivo para .env e preencha suas credenciais)
# Para Render PostgreSQL, use a "Internal Database URL" do dashboard
DATABASE_URL=postgresql://user:password@host:port/database

# JWT Secret (gere uma string aleatória segura)
JWT_SECRET=sua_chave_secreta_jwt

# Porta do servidor
PORT=3001
